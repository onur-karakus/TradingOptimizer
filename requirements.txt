# requirements.txt
# --- Proje bağımlılıklarını yönetmek için ---
# Bu dosyayı kullanarak gerekli paketleri kurmak için terminalde şu komutu çalıştırın:
# pip install -r requirements.txt

Flask
requests
Flask-APScheduler
Flask-SQLAlchemy